import * as dotenv from 'dotenv';
// Initialize .env before anything else
dotenv.config();

// if (!process.env.JWT_SECRET) {
//   throw Error('JWT_SECRET is not defined');
// }

// if (!process.env.IDX_VALIDATE_TOKEN_URL) {
//   throw Error('IDX_VALIDATE_TOKEN_URL is not defined');
// }
